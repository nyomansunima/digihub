{
  "name": "digihub",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "start": "turbo run start",
    "preview": "turbo run preview",
    "build": "turbo run build"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^1.10.12"
  },
  "dependencies": {
    "@hookform/resolvers": "^3.2.0",
    "yup": "^1.2.0"
  }
}
